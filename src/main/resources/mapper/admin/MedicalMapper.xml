<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.hospital.admin.mapper.MedicalMapper">
	<select id="mlist" resultType="kr.co.hospital.admin.dto.MedicalDto">
		select * from medical
		where ${stype} like concat('%',#{sword},'%')
		order by medi_id desc limit #{index},10
	</select>
	<select id="getChong" resultType="int">
		select ceil(count(*)/10) from medical
		where ${stype} like concat('%',#{sword},'%')
	</select>
	<select id="getJumin" resultType="string">
		select user_jumin from user where user_id=#{userid}
	</select>
</mapper>